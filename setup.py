from setuptools import setup, find_packages

setup(
    name="configlib",
    version="0.1.0",
    description="A simple configuration parser and validator",
    author="Your Name",
    packages=find_packages(),
    install_requires=[
        "PyYAML"
    ],
)
